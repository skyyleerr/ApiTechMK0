/**
 * DTOs de entrada: forma de los datos que la API recibe (por ejemplo,
 * al crear o actualizar un recurso). Nunca exponen campos gestionados
 * por el sistema (id, fechas de auditoria, password en texto plano).
 */
package com.apitech.mk5.dto.request;
