/**
 * Validadores reutilizables para reglas cruzadas (multi-tabla) que Bean
 * Validation no cubre por si sola -- el equivalente en Java a los
 * triggers de la base de datos (coherencia empresa-usuario,
 * empresa-colmena, tipo de sensor-medicion, etc.).
 */
package com.apitech.mk5.validation;
